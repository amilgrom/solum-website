---
phase: 01-foundation
type: execute
domain: next-js
---

<objective>
Create a design system and reusable layout components for consistent, professional styling across the site.

Purpose: Establish visual language and component patterns that all future content pages will use.
Output: Design tokens (colors, typography, spacing), reusable layout components (Header, Footer, Card, etc.), and Tailwind configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define design tokens and Tailwind configuration</name>
  <files>tailwind.config.ts, app/globals.css, lib/design-tokens.ts</files>
  <action>Create a professional design system for a CRE company website. In tailwind.config.ts: define a custom color palette (primary: professional blue, secondary: accent, grays for text/borders, success/warning/error), set font family to a professional sans-serif (e.g., Inter or Helvetica Neue via Google Fonts), define spacing scale (4px base unit), and border radius values. Create lib/design-tokens.ts exporting TypeScript constants for colors, fonts, and spacing. In app/globals.css: set root CSS variables matching Tailwind theme, define base styles for typography (headings, body text, links), and utility classes. Focus on conveying stability, professionalism, and trustworthiness.</action>
  <verify>npm run build succeeds. Tailwind CSS compiles without errors. Color palette is visible when reviewing globals.css and tailwind.config.ts. No unused CSS utilities in output.</verify>
  <done>Design tokens defined. Tailwind configured with professional color palette and typography. CSS variables set. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable layout components (Header, Footer, Container, Card)</name>
  <files>app/components/Header.tsx, app/components/Footer.tsx, app/components/Container.tsx, app/components/Card.tsx</files>
  <action>Build TypeScript React components that establish site-wide layout patterns. Header: logo/site title, navigation placeholder, responsive mobile menu trigger. Footer: company name, contact info placeholder, social links placeholder, copyright. Container: responsive max-width wrapper with consistent padding. Card: shadow, rounded corners, padding, hover state (for future content). Each component uses Tailwind utility classes from the design system. Include JSX comments explaining props and usage. All components are exported and ready for import in future pages.</action>
  <verify>npm run build succeeds. No TypeScript errors in components. Components render without errors when imported into page.tsx. Responsive design works at mobile (375px) and desktop (1440px) breakpoints in next dev preview.</verify>
  <done>Header, Footer, Container, and Card components created. All render correctly. Responsive breakpoints tested. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3a: Update homepage to use layout components</name>
  <files>app/page.tsx, app/layout.tsx</files>
  <action>Refactor app/page.tsx to use newly created Header, Footer, and Container components. Update app/layout.tsx to include the design system (globals.css import, metadata for site title/description). Homepage should: import Header, Footer, Container; wrap main content in Container; include a hero section with headline "Solum Real Estate Group" and subheading "Professional CRE Investing" (placeholder); ensure layout is visually appealing and conveys professionalism. Verify all files are ready for git commit.</action>
  <verify>npm run build succeeds. `npm run dev` shows homepage with Header, Footer, and layout components rendering correctly.</verify>
  <done>Homepage updated to use design system. All components render. Files ready for commit.</done>
</task>

<task type="auto">
  <name>Task 3b: Git commit for design system changes</name>
  <files>tailwind.config.ts, app/globals.css, lib/design-tokens.ts, app/components/*, app/page.tsx, app/layout.tsx</files>
  <action>Stage all design system files with `git add .`. Create commit with message "feat: add design system and layout components" followed by Claude Code attribution.</action>
  <verify>`git status` shows clean working directory. `git log` shows new commit for design system.</verify>
  <done>Design system changes committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3c: Git push to deploy design system</name>
  <action-required>In a separate terminal, change network connectivity if needed, then run:

  ```bash
  git push
  ```

  Wait for the push to complete and GitHub Actions workflow to start. Restore network connectivity when done.
  </action-required>
  <resume-signal>Type "push-complete" when git push is done, workflow is running, and network connectivity is restored for Claude API access</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated site with design system deployed</what-built>
  <how-to-verify>
    1. Check GitHub Actions tab in repository to see workflow status
    2. Wait for workflow to complete (may take 1-2 minutes)
    3. Verify: Workflow shows green checkmark (success)
    4. Visit https://solumreg.com
    5. Verify: Updated design is visible with new Header, Footer, and design system
    6. Verify: No console errors or broken assets
  </how-to-verify>
  <resume-signal>Type "deployment-verified" when site is live with updated design</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run build` completes without errors
- [ ] Tailwind CSS compiles with no warnings
- [ ] All components (Header, Footer, Container, Card) render without errors
- [ ] Design tokens match professional CRE brand expectations
- [ ] Responsive design works at mobile and desktop
- [ ] Site at solumreg.com reflects design updates
</verification>

<success_criteria>

- Design system established with professional color palette, typography, and spacing
- Reusable layout components created (Header, Footer, Container, Card)
- Tailwind configuration complete with custom tokens
- Homepage refactored to use new components
- Site deployed with design system in place
- No build errors or TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`

# Phase 1 Plan 2: Design System Summary

**Professional design system established with reusable layout components**

## Accomplishments

- Created professional color palette and typography system
- Built reusable layout components (Header, Footer, Container, Card)
- Configured Tailwind CSS with custom design tokens
- Updated homepage to use new design system
- Site deployed with professional visual identity

## Files Created/Modified

- `tailwind.config.ts` - Design token configuration
- `app/globals.css` - Base styles and CSS variables
- `lib/design-tokens.ts` - TypeScript design token constants
- `app/components/Header.tsx` - Header component
- `app/components/Footer.tsx` - Footer component
- `app/components/Container.tsx` - Container wrapper
- `app/components/Card.tsx` - Card component
- `app/page.tsx` - Updated homepage
- `app/layout.tsx` - Updated root layout

## Decisions Made

- **Color Palette**: Professional blue/gray scheme chosen to convey stability and trustworthiness
- **Typography**: Industry-standard sans-serif (via Google Fonts)
- **Component Pattern**: Prop-based reusable components for consistency

## Issues Encountered

None

## Next Phase Readiness

✅ Design system ready for content pages in Phase 2 (Company Story)

Ready to build team bios and company narrative using established components.
</output>
