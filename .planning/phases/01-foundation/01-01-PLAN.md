---
phase: 01-foundation
type: execute
domain: next-js
---

<objective>
Set up the Next.js repository, GitHub Pages hosting, and deploy pipeline for solumreg.com.

Purpose: Establish the foundational infrastructure so all subsequent phases build on a working, deployed site.
Output: Working Next.js project with GitHub Pages deployment, custom domain configured, and automated build/deploy workflow.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1a: User creates Next.js project</name>
  <action-required>Run: `npx create-next-app@latest solum-website --typescript --tailwind --app --disable-git`</action-required>
  <resume-signal>Type "done" when you've completed the Next.js project creation</resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Configure static export and initial structure</name>
  <files>next.config.js, app/page.tsx, app/layout.tsx</files>
  <action>Configure next.config.js to enable static export by setting `output: 'export'`. Remove any dynamic features (API routes, server actions). Create a minimal homepage in app/page.tsx with header and footer structure. Initialize git repo if not already done and commit initial setup.</action>
  <verify>npm run build succeeds, builds to `out/` directory with static HTML files. No errors in build output. `ls -la out/` shows index.html and other static assets.</verify>
  <done>Next.js project configured with static export. Build creates static HTML. Initial commit created. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure GitHub Pages deployment with automated workflow</name>
  <files>.github/workflows/deploy.yml, CNAME, docs/DEPLOYMENT.md</files>
  <action>Create GitHub Actions workflow file at `.github/workflows/deploy.yml` that: (1) triggers on push to main, (2) installs dependencies with `npm ci`, (3) builds with `npm run build`, (4) deploys the `out/` directory to gh-pages branch using `actions/upload-pages-artifact` and `actions/deploy-pages`. Create CNAME file with content `solumreg.com`. Document deployment process in docs/DEPLOYMENT.md. Commit workflow and CNAME.</action>
  <verify>`git push` to main triggers workflow. GitHub Actions shows successful build and deploy. GitHub Pages settings show gh-pages branch as source and solumreg.com as custom domain.</verify>
  <done>GitHub Actions workflow created and tested. CNAME configured. Deployment process documented. Site deploys on every push to main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deployed Next.js site on GitHub Pages with custom domain configured</what-built>
  <how-to-verify>
    1. Go to your GitHub repository Settings → Pages
    2. Verify: Source is set to "GitHub Actions"
    3. Verify: Custom domain shows "solumreg.com"
    4. Wait for DNS propagation (may take a few minutes)
    5. Visit https://solumreg.com (or use your registered domain if different)
    6. Verify: Page loads without errors, displays basic Next.js homepage
    7. Open browser DevTools → Console
    8. Verify: No 404 errors, no broken assets
  </how-to-verify>
  <resume-signal>Type "approved" if site loads correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `npm run build` completes without errors
- [ ] `out/` directory contains static HTML files
- [ ] GitHub Actions workflow runs successfully on push
- [ ] Site is live at solumreg.com (or configured domain)
- [ ] No TypeScript errors
- [ ] No broken links or 404s in console
</verification>

<success_criteria>

- Next.js project initialized with static export enabled
- GitHub Pages deployment configured via GitHub Actions
- Custom domain solumreg.com configured and DNS resolved
- Site deploys automatically on push to main
- No build errors or warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`

# Phase 1 Plan 1: Foundation & Infrastructure Summary

**Next.js site initialized and deployed to GitHub Pages with custom domain**

## Accomplishments

- Created Next.js project with static export configuration
- Set up GitHub Actions workflow for automated build and deploy
- Configured CNAME for custom domain (solumreg.com)
- Site now live and deploys on every push to main

## Files Created/Modified

- `package.json` - Next.js dependencies
- `next.config.js` - Static export configuration
- `app/page.tsx` - Minimal homepage template
- `.github/workflows/deploy.yml` - Automated deployment workflow
- `CNAME` - Custom domain configuration
- `docs/DEPLOYMENT.md` - Deployment documentation

## Decisions Made

- **Technology**: Next.js with static export chosen for component reuse and styling flexibility
- **Deployment**: GitHub Actions for automatic build/deploy on every push
- **Domain**: Custom domain solumreg.com configured at GitHub Pages level

## Issues Encountered

None

## Next Phase Readiness

✅ Foundation ready for Phase 2 (Company Story)

Ready to build design system and layout components in next plan.
</output>
